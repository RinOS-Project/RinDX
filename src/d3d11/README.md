# RinDX D3D11 boundary

D3D11 entry points are resolved through the `RinDxProviderV1` platform
contract.  RinDX does not claim a D3D11 implementation until the corresponding
command/resource ABI is implemented by this repository.
