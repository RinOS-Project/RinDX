# RinDX D3D12 boundary

D3D12 entry points are resolved through the `RinDxProviderV1` platform
contract.  RinDX does not claim a D3D12 implementation until the corresponding
command/resource ABI is implemented by this repository.
